<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01//EN"
   "http://www.w3.org/TR/html4/strict.dtd">
<html lang="en">
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<title>Need-o-Tron</title>
	<style type="text/css">
	    body {margin:0; font-family:helvetica;}
	    .bleed {padding-right:8mm; border-right:1px dashed #000; width:108mm; height:60mm;}
	    .label {border:solid 2mm #000;padding:0; position:relative; width:100mm; height:56mm; overflow:hidden;}
	    .label h1 {font-size:5mm; margin:0; padding:3mm 0 0 3mm;}
	    .label h2 {font-size:6mm; text-align:center; margin:0;border-bottom: solid 1mm #000;}
	    .label h3 {font-size:6mm; text-align:center; margin:0;
	        position:absolute; bottom:0;left:0; right:0; border-top: solid 1mm #000;}
	    .label p {font-size:4mm; padding:3mm;margin:0;}
	</style>
</head>
<body onload="window.print();">
	<div class="bleed">
		<div class="label">
			<h2><%= @need.kind %> - #<%= @need.id %></h2>
			<h1><%= @need.title %></h1>
			<p><%= markdown @need.description %></p>
			<h3><%= @need.named_priority %></h3>
		</div>
	</div>
</body>
</html>