
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= yield(:title) + " - " if content_for?(:title) %>GOV.UK Need-O-Tron</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag 'application' %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to 'GOV.UK Need-O-Tron', root_path, :class => "brand" %>

          <div class="search pull-right">
            <form action='<%= (controller_name == 'search') ? "" : "/search"  %>'>
              <label>Search for:
                <%= text_field_tag :query, params[:query] %>
              </label>
              <%= submit_tag "Search" %>
            </form>
          </div>

          <div class="btn-group pull-right">
            <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
              <i class="icon-user"></i> <%= current_user.name.present? ? current_user.name : current_user.email %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><a href="#">Profile</a></li>
              <li class="divider"></li>
              <li><a href="#">Sign Out</a></li>
            </ul>
          </div>
          <div class="nav-collapse">
            <ul class="nav">
              <li class="active"><a href="/">Home</a></li>
              <li><a href="#about">About</a></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">

      <% flash.each do |k, v| %>
      <p class="<%= k %> alert-<%= k %>"><%= v %></p>
      <% end %>

      <%= yield %>

      <hr>

      <footer>
        <p>&copy; GDS. <span class="pull-right">Need-o-tron version 2.0</span></p>
      </footer>

    </div><!--/.fluid-container-->

    <script type="text/javascript" charset="utf-8" src="//ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js"></script>
    <script type="text/javascript" charset="utf-8" src="//ajax.googleapis.com/ajax/libs/jqueryui/1.8.16/jquery-ui.min.js"></script>
    <%= javascript_include_tag "/javascripts/rails", "/javascripts/application","/javascripts/data_tables/jquery.dataTables.min.js", "/javascripts/needs" %>
    <%= yield :extra_javascript %>

  </body>
</html>